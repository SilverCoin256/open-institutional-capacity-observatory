# Conference Artifact

Artifact reviewers should be able to run:

```bash
python scripts/check_release.py
```

The artifact demonstrates:

- reproducible data processing
- deterministic figures
- executable notebooks
- benchmark baselines
- release audit
- credential scan

Known limitation: v1 benchmark labels are not yet independent ground truth.
