# Final External Blockers

The following actions could not be completed without third-party account access or another person's independent action:

1. **Zenodo DOI:** no authenticated Zenodo session or token was available. The release is prepared for deposit, but no DOI is claimed.
2. **PyPI/TestPyPI:** the trusted-publishing workflow is configured, but no package-index owner or publisher configuration was available. CI builds and checks distributions, but no publication is claimed.
3. **Independent validation:** an independent researcher must run the protocol and submit a report. Internal tests and AI execution cannot count.
4. **External adoption KPIs:** a lab, professor, course, paper, and conference artifact must use or cite OICO independently before being recorded.
5. **GitHub social preview attachment:** `assets/social-preview.png` is prepared and committed, but GitHub exposes the final repository-image upload through authenticated Settings UI; the available browser session is signed out. The repository's default Open Graph image remains GitHub's generated preview until that one UI action is completed.
6. **Scholarly outreach sender identity:** a private register of 60 relevant researchers was researched locally, but no authenticated scholarly email account or verified sender identity is available in the current environment. No outreach messages were sent, and no addresses were added to the public repository.
7. **Native GitHub notification scope:** the available GitHub credential lacks the `notifications` scope. A public-API-only monitor is installed instead; it records new issues, pull requests, and releases every six hours without storing credentials.

No credential is stored in the repository. No external evidence is fabricated.
